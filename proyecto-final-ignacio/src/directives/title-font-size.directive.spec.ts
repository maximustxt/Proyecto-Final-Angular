import { TitleFontSizeDirective } from './title-font-size.directive';

describe('TitleFontSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleFontSizeDirective();
    expect(directive).toBeTruthy();
  });
});
